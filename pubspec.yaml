name: auratrace
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  geolocator: ^13.0.1
  flutter_local_notifications: ^17.2.3 
  webview_flutter: ^4.9.0
  http: ^1.2.2  # For API calls to send data to the server (optional)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # Updated to the latest version

flutter:
  uses-material-design: true

# Uncomment and add any assets or fonts as needed
# assets:
#   - images/a_dot_burr.jpeg
#   - images/a_dot_ham.jpeg


